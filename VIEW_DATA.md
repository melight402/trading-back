# Как просмотреть данные

## 🌐 Веб-интерфейс (Рекомендуется)

**Самый простой способ!**

1. Запустите сервер: `npm start`
2. Откройте браузер: `http://localhost:3001`
3. Всё! Вы увидите:
   - Список всех сделок
   - Фильтры (символ, направление, даты)
   - Статистику
   - Скриншоты с графиками

## 💾 Графические инструменты для SQLite

### 1. DB Browser for SQLite ⭐ (Рекомендую)
- **Бесплатный, простой в использовании**
- Скачать: https://sqlitebrowser.org/
- Откройте файл: `./data/trading.db`
- Полнофункциональный интерфейс для просмотра и редактирования

### 2. DBeaver
- **Универсальный, мощный**
- Скачать: https://dbeaver.io/
- Поддерживает много типов БД
- Есть Community Edition (бесплатно)

### 3. SQLite Studio
- **Легковесный, быстрый**
- Скачать: https://sqlitestudio.pl/
- Простой интерфейс

### 4. VS Code расширения
- Установите расширение "SQLite Viewer" или "SQLite"
- Откройте `.db` файл прямо в редакторе
- Удобно если уже используете VS Code

### 5. Командная строка (для опытных)
```bash
cd "/Users/paul/WebstormProjects/trading back"
sqlite3 ./data/trading.db

# В SQLite:
.tables                           # Показать все таблицы
SELECT * FROM trades LIMIT 10;    # Первые 10 сделок
SELECT * FROM binance_credentials; # Показать сохраненные креды
.schema trades                    # Структура таблицы trades
.quit                            # Выйти
```

## 📸 Просмотр скриншотов

### Через файловую систему:
```
/Users/paul/WebstormProjects/trading back/data/screenshots/
```

### Через веб-интерфейс:
- Откройте `http://localhost:3001`
- Скриншоты отображаются на карточках сделок
- Клик по скриншоту открывает его в полном размере

### Прямая ссылка:
```
http://localhost:3001/api/screenshots/[имя_файла]
```

## 📊 Примеры SQL запросов

```sql
-- Все сделки
SELECT * FROM trades ORDER BY created_at DESC;

-- Сделки по символу
SELECT * FROM trades WHERE symbol = 'BTCUSDT';

-- Статистика по направлениям
SELECT 
    direction,
    COUNT(*) as count,
    AVG(entry_price) as avg_price
FROM trades
GROUP BY direction;

-- Сделки с скриншотами
SELECT * FROM trades WHERE screenshot_path IS NOT NULL;

-- Последние 5 сделок
SELECT * FROM trades ORDER BY created_at DESC LIMIT 5;
```

